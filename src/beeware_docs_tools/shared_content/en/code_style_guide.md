Follow PEP8, but keep in mind section 0 is the most important part of it.
For a lot of cases, we use ruff, which will auto-format your code, you shoujjld have pre-commit, sometimes IDE can be setup to autorun ruff, which makes things easier.
follow US spelling for API naming, variables etc.
try to avoid utils modules as much as possible, somtikmes unavoidable. try to find somewhere to put the thing instead of a utils module.

Toga additions.
